<%= form_with(model: [@twitbot,@twitbot.tweets.build], local: true) do |form| %>
  <p>
    <%= form.label :content %> <br / >
    <%= form.text_area :content %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
